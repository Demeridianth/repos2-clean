from django.apps import AppConfig

class OilpricesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'oilprices'



# It defines the configuration for your Django app.

# This file contains an AppConfig class that Django uses to register and configure the app.

# It tells Django important info about the app, such as its name and any app-specific settings or signals.